# Required: Google Gemini API key (generation + vision)
GEMINI_API_KEY=

# Optional: OpenRouter API key (cross-family verify/judge). Without it, the
# loop degrades gracefully to gate-only results.
OPENROUTER_API_KEY=
